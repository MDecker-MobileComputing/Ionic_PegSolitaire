import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'de.mide.ionic.pegsolitaire',
  appName: 'Peg-Solitaire',
  webDir: 'www'
};

export default config;
